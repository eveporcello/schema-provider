type NewsStory {
    id: ID!
    title: String!
    description: String
    url: String!
}

enum TravisAttempts {
    FIRST
    SECOND
}

type Query {
    allNews(start: Int count: Int): [NewsStory!]!
}
